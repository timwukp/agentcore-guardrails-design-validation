{
  "name": "grx-live",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "GRX Live — the read-only validation dashboard. Renders the payload emitted by platform/build/build_site_data.py and derives nothing of its own.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router-dom": "^7.18.2"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
