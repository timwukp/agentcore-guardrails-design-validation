# COST.md — projected vs actual, by phase

*Generated by `estimate_cost.py` from `cost_model.yaml` v1. Do not edit by hand: every figure here is computed, and a number typed into this file would be exactly the unverified prose the project screens for.*

**Ceiling $95.00** · **projected $6.67** · **contingency $29.00** (worst case $35.67)

**Measured at the meter: $13.3711** for the whole project, 2026-08-01 .. 2026-09-21 (end exclusive), read 2026-09-21T15:51:30Z · **per-phase actual: not obtainable** (0 of 13 phases)

Standing authorisation is $1000/mo of project spend, so this project never needed to ask. Per `feedback_spend_authorization` the authorisation removes the question, not the disclosure — hence this file.

CORRECTED 2026-09-21, by measurement. This field used to read: "Actuals are read by resource tag Project=guardrails-doc-validation, never by service. This account carries roughly $27k/mo of unrelated spend." Three of those four claims are wrong, and the method it describes returns $0 with no error. (1) The `Project` tag key is INACTIVE in Billing -- of 261 user-defined keys exactly one is Active -- so the filter it names matches nothing; the positive control against the active key returns $23,477.99 in the same log. (2) This project uses TWO tag values, `guardrails-doc-validation` on 29 resources and `grx-validation` on 2, so no single value ever saw both halves. (3) The account carries $47,629.88 for August 2026, not $27k -- the qualifier was unquantified prose that went stale (feedback_quantify_qualifiers). What survives is the fourth claim: a service-level filter WOULD misattribute other systems' cost, and the measured size of that error is now on the record -- TWICE, because the obvious replacement for a broken tag is a rule keyed on days, and there are two such rules that fail in opposite directions. Claim a whole line if ANY of its spend lands on a project day: $70,905.69 over 1,154 lines, 5,303x the truth on the 2026-09-21T15:51:30Z reading (this one GROWS between readings, because the shared lines it sweeps in keep billing daily, and it is recomputed on every run rather than trusted from here), because this account's biggest lines are storage and request traffic billed every single day, so every project day intersects them. Claim a line only if ALL of its spend is on project days: $11.70 over 105 lines -- BELOW the ledger, and it loses the expensive part, because it cannot see a shared line at all. The orphaned 40 GB volume, the single most valuable thing this measurement found, is invisible to it. No rule keyed on days alone can work here, which is why every ledger line below carries a named basis instead. Both numbers are recomputed on every run so this paragraph cannot go stale the way the $27k one did. Actuals are read per (SERVICE, USAGE TYPE) pair -- keyed on the pair because 159 usage-type names in this window appear under more than one service, `DataTransfer-Out-Bytes` under ten of them, and a census keyed on the name alone silently sums unrelated services. Checked by tools/read_actual_spend.py against 1,309 pairs over 51 days. Per-PHASE actuals are not obtainable at all: Cost Explorer's finest retroactive granularity is a calendar day and this project ran several phases per day, so every phase carries actual_usd: null.

## Per phase

| Phase | Live | Days | Projected | Actual | May amend | Status |
|:--|:--:|--:|--:|--:|:--|:--|
| **0** claim extraction, pre-registration, corpora, stats, F1-1 bisect, F5-7a | no | 1 | $0.00 | n/a | — | complete |
| **1** ApplyGuardrail / InvokeGuardrailChecks only: F3-1..9, F8-2..8, F2-5, F10-2 | yes | 2 | $0.81 | n/a | S3.2, S7.1 | pending |
| **2** build the testbed 01_iam -> 09_traces; end-to-end benign call; assert traces | yes | 1 | $0.01 | n/a | — | pending |
| **3** F1 config surface (incl. F1-3 permit trap) + F4 truth table | yes | 2 | $0.06 | n/a | S3.1, S4.1, S7.1, S7.2, S8 | pending |
| **4** F2 determinism (4 arms) + gateway-side F3 + F3-10 + F7 | yes | 2 | $1.17 | n/a | S7.1, S6.4 | pending |
| **5** F5 red team + all mutations (watchdog active); F5-3a/3b | yes | 2 | $0.05 | n/a | S3.3 | pending |
| **5c** account-level enforcement, behind the unused-model hard gate | yes | 1 | $0.01 | n/a | — | pending |
| **6** F6 latency n=1000 x 8 arm pairs, overnight, checkpoint + backoff | yes | 2 | $3.55 | n/a | S6.1, S6.3, S6.4, S9, AppendixB | pending |
| **6b** VPC-mode runtime + ECR Public egress mutation | yes | 1 | $0.05 | n/a | — | pending |
| **7** F8-1 nine-region probe, explicit region_name | yes | 2 | $0.03 | n/a | S5.3, S8 | pending |
| **8** F3-11 regression re-runs at +7d and +30d | yes | 3 | $0.94 | n/a | S3.2, S7.1 | pending |
| **9** analysis, figures, bilingual v1.3, NDA release-gate pass | no | 1 | $0.00 | n/a | — | pending |
| **99** teardown + tag sweep asserting zero survivors | yes | 1 | $0.00 | n/a | — | pending |
| | | | **$6.67** | **n/a** | | |

`Actual` reads **n/a**, not $0.00, and the difference is the point. Cost Explorer's finest retroactive granularity is a calendar day; this project ran several phases on most of its days, so no per-phase actual exists to be read. Every phase carried `actual_usd: 0.0` until 2026-09-21 and this table printed thirteen `$0.00`s and an `actual to date $0.00` — a column of placeholders that read as a column of measurements. The figure that *is* measurable is the whole-project one above, and it is measured, not projected.

`Days` is the number of distinct calendar days of observation, and it is derived from `May amend`, not chosen. A phase that may amend the document needs >= 2 (sealed as `validity_checks.reproduction_before_amendment`, enforced by `check_amendment_readiness.py`); `estimate_cost.py` refuses to authorise a phase that names an amendment target and declares one day.

## Actual spend, read off the meter

*Read by `tools/read_actual_spend.py (Cost Explorer, DAILY, account-wide)` at 2026-09-21T15:51:30Z, over 2026-08-01 .. 2026-09-21 (end exclusive). Every figure below is re-derived from Cost Explorer on each run of that script, which refuses the ledger if a line has drifted, if a metered day is neither claimed nor excluded, or if a usage type in one of this project's services is missing from the table altogether.*

Against a $6.67 projection and a $95 ceiling: inside the ceiling by 7x, and TWICE the projection. The gap is not a pricing error — it is three whole families the model never had a line for. EC2 compute ($3.4969), the EBS volume ($4.0912) and Polly ($2.2613) sum to $9.85, more than the entire projection. A cost model's omissions dominate its arithmetic.

Each get_cost_and_usage call is billed $0.01. The census itself is 17 calls ($0.17), and the running total for measuring this project is ~$0.79 (79 requests), counted in `session-logs/actual-spend-ledger-20260921.log` — the census, the tag experiments, the service-name resolution and the re-read that produced the `read_at` above. This figure only ever grows, so it is a running total with a named source rather than a fact about the project (feedback_perishable_claim_cannot_be_checked); it read $0.45 before the stamp work forced a second census. Re-checks themselves cost nothing: `--census-json` replays the saved census offline, which is also what makes the ledger re-derivable without paying the meter again. Disclosed because a measurement that hides its own price is the thing this project criticises elsewhere.

Attribution rests on 15 calendar days this project's own artifacts place it on, across 8 services it provably called. The days are derived from the repository, not listed by hand, so a ledger line claiming a day nothing happened on is refused.

| Usage type | Service | Basis | USD |
|:--|:--|:--|--:|
| `USE1-Guardrail-ContentPolicyUnitsConsumed` | Amazon Bedrock | day_set | $2.1267 |
| `USE1-GuardrailChecks-ContentFilterCheckUnitsConsumed` | Amazon Bedrock | day_set | $0.6840 |
| `USE1-Guardrail-SensitiveInformationPolicyPaidUnitsConsumed` | Amazon Bedrock | day_set | $0.1550 |
| `USE1-Guardrail-TopicPolicyUnitsConsumed` | Amazon Bedrock | day_set | $0.1114 |
| `USE1-Guardrail-ContextualGroundingPolicyUnitsConsumed` | Amazon Bedrock | day_set | $0.0129 |
| `USE1-GuardrailChecks-PromptAttackCheckUnitsConsumed` | Amazon Bedrock | day_set | $0.0368 |
| `USE1-Guardrail-WordPolicyUnitsConsumed` | Amazon Bedrock | day_set | $0.0000 |
| `USE1-Policy:Consumption-based:Authorization-API-Invocations` | Amazon Bedrock AgentCore | day_set | $0.3363 |
| `USE1-Policy:Consumption-based:Generation-Input-Tokens` | Amazon Bedrock AgentCore | day_set | $0.0175 |
| `USE1-NovaMicro-input-tokens` | Amazon Bedrock | day_set | $0.0069 |
| `USE1-NovaMicro-output-tokens` | Amazon Bedrock | day_set | $0.0205 |
| `BoxUsage:t3.small` | Amazon Elastic Compute Cloud - Compute | day_set | $3.4969 |
| `EBS:VolumeUsage.gp3` | EC2 - Other | resource_match | $4.0912 |
| `CPUCredits:t3` | EC2 - Other | day_set | $0.0134 |
| `USE1-SynthesizeSpeechGenerative-Characters` | Amazon Polly | quantity_match | $1.9198 |
| `USE1-SynthesizeSpeechNeural-Characters` | Amazon Polly | quantity_match | $0.3415 |
| | | **attributed to this project** | **$13.3711** |

And **9 lines this project will not claim**, with their real amounts, because a rejection with no number attached cannot be checked:

| Usage type | Service | USD at the meter | Why not claimed |
|:--|:--|--:|:--|
| `APS1-Guardrail-ContentPolicyUnitsConsumed` | Amazon Bedrock | $0.0000 | 481 content-policy units, $0.072150, in ap-southeast-1 on 2026-08-23 — a day this project's artifacts DO cover, which is exactly why it is worth recording as a refusal. Two fact... |
| `BoxUsage:t3.micro` | Amazon Elastic Compute Cloud - Compute | $0.0000 | 2.0694 hours, $0.021522, on 2026-08-13 — one on-demand t3.micro in us-east-1, since 2.0694 x $0.0104 reproduces the charge to six decimals, so this is a single instance and not... |
| `USE1-Knowledge-Base:Consumption-based:Retrieval` | Amazon Bedrock AgentCore | $0.0000 | 6 retrieval units, $0.006, on 2026-08-13. Not claimed: no code path in this repository calls a retrieval API. `Retrieve` / `RetrieveAndGenerate` / `bedrock-agent-runtime` appear... |
| `USE1-Knowledge-Base:Consumption-based:AgenticRetrieval` | Amazon Bedrock AgentCore | $0.0000 | 1 agentic-retrieval unit, $0.004, on 2026-08-13, same service and same argument as the line above. Recorded separately because the meter keys them separately and a ledger that m... |
| `Requests-Tier1` | Amazon Simple Storage Service | $0.0000 | S3 PUT/COPY/POST/LIST requests: $1,855.19 across the window, on all 51 days. Every `publish_web.py` upload is in here and so is everything else this account does. The payload bu... |
| `Requests-Tier2` | Amazon Simple Storage Service | $0.0000 | S3 GET/HEAD: $519.72 across the window, on all 51 days. Same boundary. |
| `TimedStorage-ByteHrs` | Amazon Simple Storage Service | $0.0000 | S3 storage: $2,007.82 across the window. The payload is a few hundred MB against an account holding terabytes. Note what the usage-type NAME does not tell you: this same name is... |
| `US-DataTransfer-Out-Bytes` | Amazon CloudFront | $0.0000 | CloudFront egress to viewers, and the one shared line with a real upper bound: the ACCOUNT-WIDE figure for this usage type over the whole window is $0.0000. The four published v... |
| `Lambda-GB-Second` | AWS Lambda | $0.0000 | $1.1493 account-wide. This project's echo Lambda (tagged `Project=guardrails-doc-validation`) is one of several functions in the account and its share is not separable at day gr... |

The largest of these dwarf the project's entire spend. That is the finding, not an inconvenience: a day rule over this account's shared lines would have attributed hundreds of dollars of unrelated storage and request traffic to a guardrails study, and the attribution note above records the measured size of that error.

## What the replication requirement cost

Nothing, in eight of the ten live phases — and the reason is a design decision that is worth stating plainly, because the obvious reading of "reproduce on two separate days" is "run it twice", which would have doubled the most expensive phase in the project.

For a phase whose n counts REPEATED TRIALS of one condition (latency, determinism) or EXCHANGEABLE CORPUS ITEMS (detection), the pre-registered n is DEALT ACROSS the two days rather than run twice. The pooled estimator keeps the pre-registered n, so the primary analysis is unchanged and the marginal cost of replication is zero; what the split buys is a between-day comparison the single-day design could not make at any n. Doubling would be the right answer only if the second day had to re-estimate the quantity, and it does not — it has to test the quantity's durability, which is a different and cheaper question.

The two phases where it is not free:

- **Phase 7** (nine-region probe) genuinely doubles: each region contributes one existence observation, so there is no *n* to deal across days. The calls are unmetered control-plane calls, so the doubling costs $0 and roughly ten minutes.
- **Phase 6** repeats its 20-call warm-up per night per arm, +320 calls. Immaterial against 16,640.

And two where the requirement does not apply and the phase is deliberately *not* replicated: **5c** (account-level enforcement — the highest blast radius in the project; where the rule does not bind, the risk-minimising choice wins) and **6b**. Both are confirm-only. If either falsifies it acquires the requirement at that moment, which is what the contingency lines below are for.

## The day-effect protocol, pre-specified

Pre-specified before data, so it is not a post-hoc knob. Test day 1 vs day 2 (Mann-Whitney U for latency, Fisher exact for proportions) at alpha = 0.01, the same level as the pre-registered cold-start contamination check. p >= 0.01: pool and report the pooled estimate. p < 0.01: do NOT pool; report per-day estimates and amend the document with a RANGE plus an explicit "varies between observation days" caveat. A day effect is a finding about the service, not a defect in the harness.

### And its limitation, stated rather than buried

Stated as a limitation, not hidden: a between-day comparison at n/2 per day is a SCREEN, not a powered test. At 2 x 500 latency trials it detects a shift in the median of roughly 0.2 sigma; a 5% recall shift at 2 x 60 corpus items it will usually miss. The split therefore excludes a GROSS transient (an outage, a throttled window, a mid-deploy service state), which is what the rule exists for, and does not license a claim that the two days are statistically indistinguishable.

## Contingency

Named and bounded, not a slush figure — each line states the observation that would draw it.

| Trigger | Amount |
|:--|--:|
| **6b-replication** — Phase 6b falsifies §4.5's egress requirement, acquiring the 2-day rule | $5.00 |
| **5c-replication** — Phase 5c falsifies §4.4 non-bypassability. Funded, but a second exposure requires an explicit decision on blast radius, not just budget. | $1.00 |
| **6-day-effect** — Phase 6's day-effect test rejects at alpha=0.01, so the two nights cannot be pooled and a third night is needed to tell a trend from two-point noise. | $15.00 |
| **retry-tail** — Throttling forces retries beyond the 3-attempt backoff, re-running trials. Sized at roughly 20% of the metered phases. | $8.00 |
| | **$29.00** |

## Unit prices

| Price | USD | Unit | Verified |
|:--|--:|:--|:--:|
| `guardrail_text_unit` | 0.0001500 | 1 text unit (<= 1000 characters) through ApplyGuardrail content filters | yes |
| `guardrail_checks_content_text_unit` | 0.0000700 | 1 text unit through InvokeGuardrailChecks contentFilter | yes |
| `guardrail_checks_prompt_attack_text_unit` | 0.0000800 | 1 text unit through InvokeGuardrailChecks promptAttack | yes |
| `guardrail_pii_text_unit` | 0.0001000 | 1 text unit through ApplyGuardrail sensitive-information filters (paid tier) | yes |
| `lambda_invoke` | 0.0000002 | 1 Lambda request — the deterministic echo target | yes |
| `lambda_gb_second` | 0.0000133 | 1 GB-second of Lambda compute (cheapest tier) | yes |
| `spans_ingest_gb` | 0.5000000 | 1 GB of custom log data ingested (Standard log class) — the aws/spans path | yes |
| `logs_insights_gb_scanned` | 0.0050000 | 1 GB scanned by a Logs Insights query | yes |
| `logs_storage_gb_month` | 0.0300000 | 1 GB-month of log storage | yes |
| `ebs_gp3_gb_month` | 0.0800000 | 1 GB-month of gp3 volume, us-east-1 | yes |
| `control_plane_call` | 0.0000000 | 1 control-plane API call (Create/Get/Update/List/Delete) | yes |

All prices verified against the AWS Pricing API.

---

*Ceiling source: ~/.claude/plans/melodic-hatching-seal.md, Part 7: "Cost disclosure: $55–95 projected (default path + 5c + 6b)". The upper end is the ceiling; the lower end is not a target.*

